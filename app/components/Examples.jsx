var React = require('react');

var Examples = (props) => {
	return (
		<div>
			<h3>Examples</h3>
			<p>Welcome to the examples page. Where are the cats.</p>
		</div>
	);
};

module.exports = Examples;